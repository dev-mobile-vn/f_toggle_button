name: f_toggle_button
description: Create toggle button.
version: 0.0.4
publish_to: 'none'
repository: "https://github.com/josephtruong/f_toggle_button"

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  loading:
    git:
      url: https://github.com/josephtruong/loading

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
