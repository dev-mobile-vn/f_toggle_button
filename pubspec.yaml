name: f_toggle_button
description: Create toggle button.
version: 0.0.5
publish_to: "https://pub.dev"
repository: "https://github.com/dev-mobile-vn/f_toggle_button"

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
